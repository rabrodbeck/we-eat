# WeEat Workspace Rules

- **Git Commit Messages**: Always use the **Conventional Commits** format (e.g., `feat(backend): ...`, `test(frontend): ...`, `docs: ...`) for Git commit messages and implementation plans.
